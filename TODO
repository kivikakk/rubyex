Game plan:
 * IO
 * require
 * catch/throw (not keywords)
 * attr_*
 * *_eval
 * regexps -- oniguruma

Minor things:
 * our float range sucks (double). Fix.
 * investigate strange issue with Array.new(int)
 * classes/modules nested
